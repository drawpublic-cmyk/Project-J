# 존급 배포 가이드 / 체크리스트

## 1. 현재 구조 한눈에
```
[브라우저] index.html (정적 1파일)
   ├─ 카카오맵 JS SDK      → 클라이언트에서 직접 호출 (JS키, 도메인 제한)
   │     └ 편의점/지하철/화장실/프린터 검색
   ├─ 공공데이터 TAGO(버스) → CORS 막힘 → proxy.js(서버) 경유 필요
   └─ config.local.js      → 로컬 전용 키 주입(.gitignore)
```
- **정적 호스팅만으로 되는 것**: 지도 + 편의점·지하철·화장실·프린터
- **백엔드(프록시)가 있어야 되는 것**: 버스(공공데이터)

---

## 2. 배포에 필요한 것 (체크리스트)

### A. 정적 프론트 호스팅
- [ ] GitHub Pages / Netlify / Vercel / Cloudflare Pages 중 택1
- [ ] **HTTPS 필수** (위치 권한 geolocation은 https 또는 localhost에서만 동작)

### B. 카카오맵
- [ ] 카카오 JavaScript 키 (도메인 제한이라 클라이언트 노출 OK)
- [ ] **카카오 콘솔 → 플랫폼(Web) → 사이트 도메인에 배포 도메인 등록** ← 이거 안 하면 `domain mismatched` 로 지도 안 뜸
  - 예: `https://drawpublic-cmyk.github.io`, 커스텀 도메인 쓰면 그것도 추가

### C. 공공데이터(버스) — ⚠️ 백엔드 필요
- [ ] data.go.kr 서비스키 (`DATA_GO_KR_KEY`) — **도메인 제한 없음 → 절대 클라이언트/공개레포에 노출 금지**
- [ ] CORS 우회 + 키 은닉용 **서버/서버리스 프록시**
  - 로컬: `proxy.js`(`node proxy.js`)
  - 배포: 서버리스 함수(Vercel/Netlify Functions, Cloudflare Worker)로 대체 권장
- [ ] (보안 강화) 클라이언트는 키 없이 프록시만 호출 → **프록시가 서버에서 키 주입** (현재는 클라이언트가 키를 들고 프록시로 보냄 → 배포 시 이 구조 변경 권장)

### D. 시크릿 관리
- [ ] `config.local.js` 는 배포에 포함하지 않음(.gitignore)
- [ ] 카카오 JS키: 공개 가능(도메인 제한). 단 카카오 콘솔 도메인 allow-list를 좁게 유지
- [ ] data.go.kr 키: 호스팅/함수의 **환경변수(Secret)** 로만 주입

---

## 3. 배포 시나리오 (난이도순)

### 시나리오 ①  GitHub Pages만 (가장 쉬움, 버스 제외)
- 지금 그대로. 지도 + 편의점/지하철/화장실/프린터 동작
- 버스는 mock (프록시 없음)
- 할 일: 카카오 콘솔에 `https://drawpublic-cmyk.github.io` 등록

### 시나리오 ②  Vercel/Netlify (정적 + 서버리스 프록시) — ⭐ 권장
- 정적 파일 + `/api/bus` 같은 서버리스 함수로 `proxy.js` 역할 대체
- `DATA_GO_KR_KEY` 는 함수 환경변수로 (클라이언트 비노출)
- **버스까지 전부 동작**, 무료 티어로 가능
- 할 일: 함수 1개 작성 + 환경변수 설정 + 카카오 도메인에 배포 URL 등록

### 시나리오 ③  단독 서버(VPS/Docker)
- nginx(정적) + node(proxy.js) 같이 구동, 풀 제어
- 도메인/HTTPS(Let's Encrypt) 직접 관리

---

## 4. 배포 전 정리할 항목 (현재 미완/주의)
- [ ] **버스 도착정보·혼잡도**: 아직 샘플. data.go.kr `버스도착정보(ArvlInfoInqireService)` 활용신청 후 연동
- [ ] **서울 버스정류장**: TAGO 커버리지 약함 → 서울은 TOPIS(서울시 정류소 API) 보완 필요
- [ ] **매장 사진**: 회색 placeholder (실데이터 교체 예정)
- [ ] **즐겨찾기**: localStorage 기반(브라우저 로컬). 계정 연동하려면 백엔드 필요
- [ ] 정적 자산 캐싱/버전(쿼리스트링) — 업데이트 반영 위해

---

## 5. 요약
- **지도만 빨리 공개** → 시나리오① (카카오 도메인 등록만 하면 끝)
- **버스까지 풀기능 공개** → 시나리오②(서버리스 프록시) 필요. 원하면 `/api/bus` 함수 + 클라이언트 수정까지 만들어줄 수 있음.
