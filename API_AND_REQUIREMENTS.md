# 존급 프로토타입 검토 메모

## ⚡ 빠른 셋업 (API 키 넣기)

`index.html` 하단 스크립트의 `CONFIG` 객체만 채우면 됩니다. 비워두면 전부 mock 으로 동작합니다.

```js
const CONFIG = {
  KAKAO_JS_KEY: "여기에_카카오_JavaScript_키",  // 실제 지도 + 주변 장소 검색
  DATA_GO_KR_KEY: "여기에_공공데이터_디코딩_서비스키", // 화장실 / 버스 혼잡도
  PROXY: "http://localhost:8787/",  // 공공데이터 CORS 우회 (proxy.js 실행 시)
  DEFAULT_CENTER: { lat: 37.5547, lng: 126.9707 },
  RADIUS: 500
};
```

### 1) 카카오맵 (지도 + 편의점·화장실·프린터·버스 검색)
1. https://developers.kakao.com → 내 애플리케이션 → 앱 생성
2. **JavaScript 키** 복사 → `KAKAO_JS_KEY` 에 입력
3. 앱 설정 → 플랫폼 → **Web** 에 사이트 도메인 등록 (로컬은 `http://localhost:포트`)
4. 키가 있으면 mock 지도가 실제 카카오맵으로 자동 전환되고, 필터에 따라 `services.Places` 로 반경 검색합니다.
   - 편의점 = 카테고리 `CS2`, 화장실/프린터/버스 = 키워드 검색
   - 핀 색은 Spec 그대로(카테고리=라벤더, 버스=초록) CustomOverlay 로 그립니다.

### 2) 공공데이터포털 (화장실 표준데이터 / 서울 버스 혼잡도)
1. https://www.data.go.kr 가입 후 아래 API 활용신청
   - 전국공중화장실표준데이터, 서울특별시 버스도착정보조회 서비스
2. 마이페이지에서 **일반 인증키(Decoding)** 복사 → `DATA_GO_KR_KEY`
3. 브라우저 CORS 차단 때문에 프록시 필요:
   ```bash
   node proxy.js          # → http://localhost:8787/ 에서 실행
   ```
   그다음 `CONFIG.PROXY = "http://localhost:8787/"` 설정. 끝.

### 3) 로컬 실행
```bash
cd outputs/jongeup-prototype
python3 -m http.server 4173   # http://127.0.0.1:4173/
```

> 운영 배포 시에는 키를 프론트에 직접 넣지 말고 `proxy.js` 같은 백엔드에서 주입하세요. (아래 "서버/DB가 필요한 지점" 참고)

---

## PDF에서 확인한 화면

- 스플래시: 핀 + "급" 로고, "존나 급할 때, 존급"
- 온보딩: 위치 권한 허용 유도
- 메인 지도: 전체/화장실/편의점/프린터/버스정류장 필터, SOS 버튼
- 리스트: 반경 500m, 가까운 순 정렬, 카테고리별 결과
- 상세: 화장실 거리, 도보 시간, 개방 상태, 성별, 휠체어, 비밀번호, 비데, 청결도
- 길찾기: 목적지까지 남은 거리/시간, 안내 중단
- 검색: 최근 검색, 검색 결과
- 버스 상세: 정류장별 도착 예정, 노선, 혼잡도
- 긴급 모드: 가장 가까운 화장실을 바로 안내

## 현재 프로토타입

- 서버, DB, API 없이 정적 HTML 하나로 동작
- 데이터는 프론트엔드 mock 배열로 구성
- 필터, 검색 화면 이동, 상세, 버스 상세, SOS, 길안내 흐름 클릭 가능
- 로컬 테스트 URL: `http://127.0.0.1:4173/`

## API 후보

### 화장실

- 후보: 공공데이터포털 `전국공중화장실표준데이터`
- 장점: 전국 단위, 화장실명/주소/개방시간/시설 정보 제공
- 주의: 2025년 2월 이후 원천데이터 정책 변경으로 WGS84 위도/경도 제공이 중단되었다고 명시됨
- 필요 작업: 주소를 좌표로 바꾸는 지오코딩 + 좌표 캐시 DB 필요
- 구현(현재): `구분` 필드로 **공중화장실 / 개방화장실**(민간 개방형 건물 화장실)을 모두 수집.
  좌표가 없어 `소재지도로명주소`를 **카카오 지오코더로 변환 + localStorage 캐시**(`jongeup_geo_v1`).
  설정: `CONFIG.TOILET_DATA_URL` 에 활용신청 후 받은 odcloud `/v1/uddi/<uuid>` **전체 URL**을 입력
  (계정마다 uuid가 달라 코드에 하드코딩 불가). 좌표변환 비용 때문에 한 번에 최대 40건만 처리(MVP).
- 한계: **개방 신청·등록된 건물만** 데이터에 존재. 미등록 건물 화장실은 어떤 API로도 안 잡힘.

### 편의점 / 장소 검색

- 후보: Kakao Local API
- 사용처: 키워드 검색, 카테고리 검색, 반경 검색
- 편의점은 카카오 장소 카테고리/키워드 검색으로 MVP 구현 가능

### 프린터

- 후보: Kakao/Naver 장소 키워드 검색으로 `프린트`, `출력`, `인쇄`, `무인프린트` 검색
- 주의: 전국 단위로 안정적인 공공 표준 API는 추가 확인 필요
- 현실적 MVP: 키워드 검색 결과 + 사용자 제보 + 관리자 검수 DB

### 버스정류장 / 혼잡도

- 후보: 공공데이터포털 `서울특별시_버스도착정보조회 서비스`
- 사용처: 버스 도착 예정, 예측 도착 시간, 차량번호, 버스 유형, 혼잡도, 만차 여부
- 주의: 서울 중심 API이므로 전국 확장 시 지역별 버스 API 통합 계층 필요

### 지도 / 길안내

- MVP: 네이버 지도/카카오맵 앱 스킴으로 외부 앱 길안내 실행
- 고도화: 지도 SDK + 서버 프록시 + 모빌리티/경로 API 검토

## 서버/DB가 필요한 지점

- 장소 통합 인덱스: 화장실, 프린터, 제보 장소, 보정 좌표 저장
- API 키 보호: Kakao/Naver/공공 API 키를 앱에 직접 넣지 않기 위한 백엔드 프록시
- 캐싱: 공공데이터/지오코딩/버스 정류장/편의시설 결과 캐시
- 사용자 제보: 장소 추가, 영업시간 수정, 비밀번호/청결도/폐쇄 신고
- 운영 검수: 신고 데이터 승인/반려, 중복 병합, 부정확 좌표 수정
- 로그/분석: 검색어, SOS 사용 흐름, 실패 장소, 지역별 데이터 부족 구간

## MVP 우선순위

1. 현재 위치 기반 500m 반경 지도/리스트
2. 화장실 + 편의점 우선 제공
3. SOS는 가장 가까운 열린 화장실로 바로 연결
4. 길안내는 외부 지도 앱 실행
5. 프린터와 버스 혼잡도는 2차 기능으로 확장

## 참고한 공식/공공 문서

- 공공데이터포털 전국공중화장실표준데이터: https://www.data.go.kr/data/15012892/standard.do
- 공공데이터포털 서울특별시 버스도착정보조회 서비스: https://www.data.go.kr/data/15000314/openapi.do
- Kakao Local API 문서: https://developers.kakao.com/docs/latest/ko/local/dev-guide
